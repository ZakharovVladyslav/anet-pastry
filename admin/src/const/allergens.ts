import { EAllergens } from '@/enums/allergens';

export const allergens: EAllergens[] = [
   EAllergens.BERRIES,
   EAllergens.CHOCOLATE,
   EAllergens.EGGS,
   EAllergens.FRUCTOSE,
   EAllergens.GLUTEN,
   EAllergens.LACTOSE,
   EAllergens.NUTS,
   EAllergens.PROTEIN,
];

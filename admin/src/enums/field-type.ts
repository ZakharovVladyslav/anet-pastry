export enum EFieldType {
   TEXT = 'text',
   NUMBER = 'number',
   DATE = 'date',
   SELECT = 'select',
   BOOLEAN = 'boolean',
   FILE = 'file',
   IMAGE = 'image',
   PASSWORD = 'password',
   EMAIL = 'email',
   TEXTAREA = 'textarea',
}

export enum EShopTab {
   ORDERS = 'orders',
   PRODUCTS = 'products',
   STATISTICS = 'statistics',
}

export enum ESystemTab {
   USERS = 'users',
   ADMINS = 'admins',
   SETTINGS = 'settings',
}

export enum ETabSections {
   SHOP = 'shop',
   SYSTEM = 'system',
}
